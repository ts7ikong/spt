<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.jeecg.modules.sptsjzx.qyaqjcgl.statistics.mapper.StatSwTypeCacheMapper">

    <insert id="batchUpsert">
        INSERT INTO stat_sw_type_cache
        (company_code, county_code, park_code, yq_type, is_scqy, type_name, type_count, updated_at)
        VALUES
        <foreach collection="list" item="r" separator=",">
            (#{r.companyCode}, #{r.countyCode}, #{r.parkCode}, #{r.yqType}, #{r.isScqy},
             #{r.typeName}, #{r.typeCount}, NOW())
        </foreach>
        ON DUPLICATE KEY UPDATE
            county_code  = VALUES(county_code),
            park_code    = VALUES(park_code),
            yq_type      = VALUES(yq_type),
            is_scqy      = VALUES(is_scqy),
            type_count   = VALUES(type_count),
            updated_at   = NOW()
    </insert>

    <!-- 按企业列表聚合各大类作业票数量，供 ticketTypeStats 使用 -->
    <select id="queryTypeStats" resultType="map">
        SELECT
            type_name AS name,
            SUM(type_count) AS value
        FROM stat_sw_type_cache
        WHERE company_code IN
        <foreach collection="companyCodes" item="c" open="(" separator="," close=")">#{c}</foreach>
        <if test="yqType != null">
            AND yq_type = #{yqType}
        </if>
        <if test="isScqy != null">
            AND is_scqy = #{isScqy}
        </if>
        GROUP BY type_name
        ORDER BY value DESC
    </select>

</mapper>
